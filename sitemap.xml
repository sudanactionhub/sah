export default function handler(req, res) {
  const baseUrl = "https://www.sudanactionhub.org";


  const staticPages = ["", "news", "research", "organization-directory", "organization-directory/data", "evidence-collection", "advocacy", "events", "events/:id", "event-programming", "humanitarian", "diaspora", "gala", "donations", "donate", "contact", "about", "joinus", "privacypolicy", "blog","blog", "blog/:slug", "resources", "resources/:id"];



  const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
  <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    ${pages
      .map((path) => {
        return `
          <url>
            <loc>${baseUrl}${path}</loc>
            <lastmod>${new Date().toISOString()}</lastmod>
            <changefreq>weekly</changefreq>
            <priority>${path === "" ? "1.0" : "0.7"}</priority>
          </url>
        `;
      })
      .join("")}
  </urlset>`;

  res.setHeader("Content-Type", "text/xml");
  res.write(sitemap);
  res.end();
}